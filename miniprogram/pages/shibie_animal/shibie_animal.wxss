.all{
  margin:  10px ;
box-shadow: 0 2px 12px 0 rgba(0, 0, 0, 0.1);
border-radius: 3%;
overflow: hidden
}
/* .pages{background:rgba(71,154,199,0.1); */

/*  
} */

.face{height: 500rpx;
      overflow: hidden;
      display:flex;
      justify-content:center;
      align-items:center;
  
      }
.face image{width: 100%; height: 100%; }      

/* 识别展示 */
.result{background: white; padding: 40rpx 25rpx 30rpx 25rpx;
        display: flex; justify-content: space-between;}
.result-text{font-size: 35rpx; color:black;}
.friends{background:rgb(71,154,199,0.5);
        border-radius: 6rpx;
        padding: 3rpx 8rpx;
        color: #fff
        }


/* 结果 */
/* 可信度 */
.botany{display: flex; justify-content: space-between; font-size: 28rpx; color:black;
        padding: 30rpx 25rpx;}
.botany view{width: 33.3%;} 
.botany view:nth-child(2){text-align: center;}
.botany view:nth-child(3){display: flex; justify-content: flex-end;}       
.botany-attr:nth-child(2n){background: #f5f5f5;}
.botany-attr{padding: 30rpx 25rpx;}
.botany-attr{color: #479AC7}
.botany-view{margin-bottom: 70px;}

/* 选择图片 */
.btn{position: fixed; bottom: 30rpx; width: 100%; }
.btn button{width: 150rpx; height: 150rpx; line-height: 130rpx; font-size: 30rpx;
 border-radius: 50%; background: linear-gradient(#6699CC, #eee9e4);color: #fff;
 box-shadow: 0rpx 1rpx 5rpx 5rpx rgba(0,0,0,0.1);

 }
 .text{
   text-align: center
 }


  /* 百度百度内容 */
  .introd{position: fixed; top: 0; left: 0;
        right: 0; bottom: 0;
        background: rgba(0, 0, 0, 0.7);
        color: white;
        font-size: 32rpx;
        z-index: 999;
        }
.introd-view{
word-break: keep-all;
word-wrap: break-word;
width: 600rpx;
height: 800rpx;
margin: 50rpx auto 0 auto;
overflow-y: auto;
          }
.introd-img image{width: 150rpx; height: 150rpx;}          
.introd-img{width: 150rpx; height: 150rpx; border-radius: 50%;
              margin: 60rpx auto 0 auto;
              }

/* 动画 */
.animation{position: fixed;
          top: 0;
          left: 0;
          right: 0;
          height: 500rpx;
          overflow:hidden;}
.animation-list{width: 100%;
                height: 500rpx;
                background: linear-gradient(to bottom, rgba(216,179,255,0),rgba(216,179,255,0.7));
                position: relative;
                top: -600rpx;
                animation: myfist 2s linear 1s infinite alternate;
                }    

  /*开始执行动画  */
  @keyframes myfist
  {
    0%{background: linear-gradient(to bottom, rgba(216,179,255,0),rgba(216,179,255,1));
        left: 0; top: -600rpx;}
    25%{left: 0; top: 50rpx;}
    50%{left: 0; top: 50rpx;}
    75%{left: 0; top:50rpx;}
    100%{left: 0; top: -600rpx;}    
  }

  /* 没有识别到花 */
  .nobotany{text-align: center; font-size: 35rpx; color:black; margin-top: 40rpx;}